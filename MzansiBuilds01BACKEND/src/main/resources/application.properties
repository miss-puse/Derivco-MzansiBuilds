# ======================
# SERVER
# ======================
server.port=${SERVER_PORT:8081}
server.servlet.context-path=/api
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

# ======================
# DATABASE - H2 (Development - No Setup Required)
# ======================
spring.datasource.url=jdbc:h2:mem:mzansibuilds
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ======================
# DATABASE - PostgreSQL (Production - Uncomment to use)
# ======================
#spring.datasource.url=jdbc:postgresql://localhost:5432/mzansibuilds
#spring.datasource.username=postgres
#spring.datasource.password=your_password
#spring.datasource.driver-class-name=org.postgresql.Driver

# ======================
# DATABASE - MySQL (Production - Uncomment to use)
# ======================
#spring.datasource.url=jdbc:mysql://localhost:3306/mzansibuilds
#spring.datasource.username=root
#spring.datasource.password=your_password
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================
# JPA
# ======================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.open-in-view=false
# For PostgreSQL use: org.hibernate.dialect.PostgreSQLDialect
# For MySQL use: org.hibernate.dialect.MySQL8Dialect

# ======================
# JWT SECURITY
# ======================
# Secret must be a Base64-encoded string representing >= 256 bits (32+ bytes).
# CHANGE THIS VALUE before deploying to production!
jwt.secret=TXphbnNpQnVpbGRzMjAyNkp3dFNlY3JldEtleVhYWFg=
# Access token expiry: 24 hours (milliseconds)
jwt.expiration=86400000
# Password-reset token expiry: 1 hour (milliseconds)
jwt.reset-expiration=3600000

# ======================
# EMAIL (GMAIL SMTP)
# ======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hustleside725@gmail.com
spring.mail.password=naiouknidopsnrst
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ======================
# APP SETTINGS
# ======================
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:5173,http://localhost:3000,http://localhost:5174}

# ======================
# FILE UPLOAD
# ======================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=uploads